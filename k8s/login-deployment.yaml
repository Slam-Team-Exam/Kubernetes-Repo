apiVersion: v1
kind: Service
metadata:
  name: login
  namespace: staging
spec:
  selector:
    app: login
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP